<div>Dear <%= @order.name %></div>
<div>Thank you for your recent order from The Pragmatic Store.</div>
<p>You ordered the following items:</p>
<table>
  <tr><th colspan="2">Qty</th><th colspan="2">Description</th></tr>
  <% @order.line_items.each do |item| %>
    <tr>
      <td><%= item.quantity %></td>
      <td>x</td>
      <td><%= item.product.title %></td>
      <td><%= number_to_currency(item.quantity * item.price) %></td>
    </tr>
  <% end %>
</table>
<p>We'll send you a separate e-mail when your order ships.</p>